export const CustomPageView = ({ children }) => {
  return (
    <div
      style={{
        paddingTop: "2%",
        paddingLeft: "10%",
        paddingRight: "10%",
      }}
    >
      {children}
    </div>
  );
};
