export const CustomPageView = ({ children }) => {
  return (
    <div
      style={{
        paddingTop: "2%",
        paddingLeft: "20%",
        paddingRight: "20%",
      }}
    >
      {children}
    </div>
  );
};
